@using DWM.Models.Repositories
@model ContaReceberViewModel
@{ 
    Layout = null;
}

<div>
    @using (Ajax.BeginForm("Cancel", "Cobranca", new AjaxOptions { HttpMethod = "GET" }, new { @class = "form", @id = "form1" }))
    {
        @Html.HiddenFor(info => info.operacaoId)
        <div class="well text-center margem-topo-10">Todos os títulos em aberto serão baixados por motivo de cancelamento. Os títulos já pagos ou amortizados não sofrerão alterações.</div>
        <input type="submit" class="btn btn-danger" value="Cancelar">
    }
</div>

@*<script type="text/javascript">
    $(function () {
        $('#form1').submit(function () {
            if ($(this).valid()) {
                $.ajax({
                    url: this.action,
                    type: this.method,
                    data: $(this).serialize(),
                    success: function (result) {
                        $('#Cancelar').html(result);
                    }
                });
            }
            return false;
        });
    });
</script>*@

@using DWM.Models.Repositories;
@model System.Web.Mvc.PagedList<CentroCustoViewModel>
@{
    Layout = null;
}

@if (Model != null)
{
    <div class="row Scroll" style="margin-top: -15px">
        <div class="col-md-12" style="padding-right: 0px;">
            <table class="table table-hover table-bordered table-condensed dedo-indicador">
                <caption></caption>
                <thead class="corvo text-white">
                    <tr>
                        <th>Descrição</th>
                    </tr>
                </thead>
                @foreach (CentroCustoViewModel ccu in Model)
                {
                    <tr onclick="Move('@ccu.centroCustoId', '@ccu.descricao')">
                        <td class="col-md-7 col-sm-2">
                            <small>@ccu.descricao</small>
                        </td>
                    </tr>
                }
            </table>
        </div>
    </div>
    @Html.Partial("Paginacao", Model)
}
else
{
    @Html.Partial("_AcessoNegadoModal")
}









